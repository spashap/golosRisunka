# секреты — никогда в git
.env
# содержит приватный ключ Cloudflare Origin
scripts/deploy/install_cert.sh
# бандлы с приватными рисунками / сэмплами (golos_samples.tar.gz и т.п.) — где угодно
*.tar.gz
# исходник гео-базы DB-IP (88 МБ, скачивается отдельно; geoip.db строится из него)
scripts/dbip-city-lite-*.csv*

# данные клиентов/тестов и сгенерированные артефакты
data/
component-gallery.html
# webp-миниатюры сэмплов — генерируются в рантайме
static/img/samples/

# приватные материалы (детские рисунки с ФИО, старые сэмплы заказчика, базовый промпт)
projectSpec/testDrawings/
projectSpec/reportSamples/
# Реальные детские рисунки для фремиум-беты. Репозиторий ПУБЛИЧНЫЙ — чужие детские
# работы в него попадать не должны (та же причина, что у testDrawings выше).
projectSpec/fremium/testDraw/

# окружение
venv/
__pycache__/
*.pyc

# локальные настройки Claude Code
.claude/
projectSpec/blog/

# локальный скрипт удалённого деплоя (вход по ssh-ключу, не секрет — держим вне репо)
deploy-remote.bat

# артефакт-заглушка от старого бага release.bat (echo с '>' создавал файл 'pushed')
pushed
